# Watcher — runtime configuration
WS_PORT=7421
CLAUDE_PROJECTS_DIR=   # defaults to ~/.claude/projects

# Overlay — baked in at build time (set before running pnpm build)
VITE_WS_URL=ws://localhost:7421

# Overlay — dev server port
PORT=7422
