{
  "name": "pixeldev",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter pixeldev-watcher start & pnpm --filter pixeldev-overlay preview"
  },
  "workspaces": [
    "packages/*"
  ]
}
